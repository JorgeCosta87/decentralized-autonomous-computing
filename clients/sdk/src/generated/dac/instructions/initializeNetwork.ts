/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { DAC_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getCodeMeasurementDecoder,
  getCodeMeasurementEncoder,
  type CodeMeasurement,
  type CodeMeasurementArgs,
} from "../types";

export const INITIALIZE_NETWORK_DISCRIMINATOR = new Uint8Array([
  75, 50, 251, 50, 0, 159, 20, 5,
]);

export function getInitializeNetworkDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_NETWORK_DISCRIMINATOR,
  );
}

export type InitializeNetworkInstruction<
  TProgram extends string = typeof DAC_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountNetworkConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountNetworkConfig extends string
        ? WritableAccount<TAccountNetworkConfig>
        : TAccountNetworkConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeNetworkInstructionData = {
  discriminator: ReadonlyUint8Array;
  cidConfig: string;
  allocateGoals: bigint;
  allocateTasks: bigint;
  approvedCodeMeasurements: Array<CodeMeasurement>;
};

export type InitializeNetworkInstructionDataArgs = {
  cidConfig: string;
  allocateGoals: number | bigint;
  allocateTasks: number | bigint;
  approvedCodeMeasurements: Array<CodeMeasurementArgs>;
};

export function getInitializeNetworkInstructionDataEncoder(): Encoder<InitializeNetworkInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["cidConfig", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["allocateGoals", getU64Encoder()],
      ["allocateTasks", getU64Encoder()],
      [
        "approvedCodeMeasurements",
        getArrayEncoder(getCodeMeasurementEncoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_NETWORK_DISCRIMINATOR }),
  );
}

export function getInitializeNetworkInstructionDataDecoder(): Decoder<InitializeNetworkInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["cidConfig", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["allocateGoals", getU64Decoder()],
    ["allocateTasks", getU64Decoder()],
    ["approvedCodeMeasurements", getArrayDecoder(getCodeMeasurementDecoder())],
  ]);
}

export function getInitializeNetworkInstructionDataCodec(): Codec<
  InitializeNetworkInstructionDataArgs,
  InitializeNetworkInstructionData
> {
  return combineCodec(
    getInitializeNetworkInstructionDataEncoder(),
    getInitializeNetworkInstructionDataDecoder(),
  );
}

export type InitializeNetworkAsyncInput<
  TAccountAuthority extends string = string,
  TAccountNetworkConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  networkConfig?: Address<TAccountNetworkConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  cidConfig: InitializeNetworkInstructionDataArgs["cidConfig"];
  allocateGoals: InitializeNetworkInstructionDataArgs["allocateGoals"];
  allocateTasks: InitializeNetworkInstructionDataArgs["allocateTasks"];
  approvedCodeMeasurements: InitializeNetworkInstructionDataArgs["approvedCodeMeasurements"];
};

export async function getInitializeNetworkInstructionAsync<
  TAccountAuthority extends string,
  TAccountNetworkConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_PROGRAM_ADDRESS,
>(
  input: InitializeNetworkAsyncInput<
    TAccountAuthority,
    TAccountNetworkConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeNetworkInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountNetworkConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    networkConfig: { value: input.networkConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.networkConfig.value) {
    accounts.networkConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            100, 97, 99, 95, 110, 101, 116, 119, 111, 114, 107, 95, 99, 111,
            110, 102, 105, 103,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.authority.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.networkConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeNetworkInstructionDataEncoder().encode(
      args as InitializeNetworkInstructionDataArgs,
    ),
    programAddress,
  } as InitializeNetworkInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountNetworkConfig,
    TAccountSystemProgram
  >);
}

export type InitializeNetworkInput<
  TAccountAuthority extends string = string,
  TAccountNetworkConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  networkConfig: Address<TAccountNetworkConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  cidConfig: InitializeNetworkInstructionDataArgs["cidConfig"];
  allocateGoals: InitializeNetworkInstructionDataArgs["allocateGoals"];
  allocateTasks: InitializeNetworkInstructionDataArgs["allocateTasks"];
  approvedCodeMeasurements: InitializeNetworkInstructionDataArgs["approvedCodeMeasurements"];
};

export function getInitializeNetworkInstruction<
  TAccountAuthority extends string,
  TAccountNetworkConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DAC_PROGRAM_ADDRESS,
>(
  input: InitializeNetworkInput<
    TAccountAuthority,
    TAccountNetworkConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeNetworkInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountNetworkConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DAC_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    networkConfig: { value: input.networkConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.networkConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeNetworkInstructionDataEncoder().encode(
      args as InitializeNetworkInstructionDataArgs,
    ),
    programAddress,
  } as InitializeNetworkInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountNetworkConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeNetworkInstruction<
  TProgram extends string = typeof DAC_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    networkConfig: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeNetworkInstructionData;
};

export function parseInitializeNetworkInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeNetworkInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      networkConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeNetworkInstructionDataDecoder().decode(instruction.data),
  };
}
